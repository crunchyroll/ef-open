---
params:
  default:
    one: default one
    two: default two
    o: o
    ne: ne
    "/_-.": slashunderscoredashdot
    ".": dot
    my-thing: my-hyphen-thing
  alpha:
    blah: unused
    two: alpha two
    one: alpha one
  test:
    one: testenv one
    two: testenv two
    ENV: myenvironmentshouldnotoverride
  staging:
    one: staging one
