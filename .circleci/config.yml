version: 2.1
jobs:
  build-and-test:
    docker:
      - image: circleci/python:2.7
    steps:
      - checkout
      - run: pip install '.[test]'
      - run:
          command: python -m unittest discover
          name: Unit Tests
      - latebind/resolve-config:
          env: staging
          profile: ellationeng
          region: us-west-2
          service: test-instance
orbs:
  latebind:
    orbs:
      aws-cli: circleci/aws-cli@1.0.0
    executors:
      default:
        docker:
          - image: circleci/python:2.7
    commands:
      resolve-config:
        parameters:
          env:
            type: string
          profile:
            type: string
          region:
            type: string
            default: us-west-2
          service:
            type: string
        steps:
          - aws-cli/install
          - aws-cli/setup:
              profile-name: ellation
              aws-access-key-id: AWS_ACCESS_KEY_ID_ELLATION
              aws-region: AWS_DEFAULT_REGION
              aws-secret-access-key: AWS_SECRET_ACCESS_KEY_ELLATION
          - aws-cli/setup:
              profile-name: ellationeng
              aws-access-key-id: AWS_ACCESS_KEY_ID_ELLATIONENG
              aws-region: AWS_DEFAULT_REGION
              aws-secret-access-key: AWS_SECRET_ACCESS_KEY_ELLATIONENG
          - run:
              command: |
                aws s3 cp --profile ellation s3://ellation-cx-global-dist/latebind-resolver/bin/linux/resolve-config .
                chmod +x ./resolve-config
              name: Install latebind resolve-config
          - run:
              command: |
                files=`find latebinds -name "*.parameters.json"`
                regex='latebinds\/parameters\/(.*).parameters.json'
                for f in $files; do
                  [[ $f =~ $regex ]]
                  ./resolve-config -env <<parameters.env>> -param $f -profile <<parameters.profile>> -region <<parameters.region>> -service <<parameters.service>> -template latebinds/templates/${BASH_REMATCH[1]}
                done
              name: Run resolve-config
workflows:
  main:
    jobs:
      - build-and-test:
          context: aws
