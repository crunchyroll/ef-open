version: 2.1

jobs:
  build-and-test:
    docker:
      - image: circleci/python:2.7
    steps:
      - checkout
      - run:
          command: echo "this is a test"
          name: Unit Tests
workflows:
  main:
    jobs:
      - build-and-test
